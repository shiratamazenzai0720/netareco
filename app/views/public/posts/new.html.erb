<div class="container mt-4 mb-5">
  <div class="row justify-content-center">
    <div class="col-md-9">
      <div class="shadow rounded">
        <div class="p-3 border-bottom bg-white text-center">
          <h2 class="mb-0 mt-4">新規投稿</h2>
        </div>

        <div class="p-4 bg-white text-left">
          <% if @post.errors.any? %>
            <div class="alert alert-danger">
              <%= @post.errors.count %>件のエラーが発生しました
              <ul>
                <% @post.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
          
          <% unless current_user&.guest_user? %>
            <%= form_with model: @post do |f| %>
              <label for="inputTitle">タイトル</label>
                <%= f.text_field :title, autofocus: true, id:"inputTitle",  class: "form-control mb-3"%>
              <label for="inputbody">本文</label>
                <%= f.text_area :body, rows: 3, autofocus: true, id:"inputbody",  class: "form-control mb-3"%>
              <%= f.submit '投稿' %>
            <% end %>
          <% else %>
          <p>ゲストユーザーは新規投稿ができません。</p>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>