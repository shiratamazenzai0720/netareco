<!DOCTYPE html>
<html>
  <head>
    <title>Netareco</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/161e2fa7d8.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

    
    
    <% if admin_signed_in? %>
      <%= render 'layouts/header_admin' %>
    <% elsif user_signed_in? %>
      <%= render 'layouts/header_public' %>
    <% else %>

  <body class="d-flex flex-column vh-100">
    <header class="shadow-sm">
      <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
          <a class="navbar-brand p-3" href="/"><%= image_tag('icon.jpg', size: "180x70") %></a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                        aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
            <li class="px-4">
              <%= link_to "新規登録", new_user_registration_path %>
            </li>
            <li class="px-4">
              <%= link_to "ログイン", new_user_session_path %>
            </li>
            <li class="px-4">
              <%= link_to "ゲストログイン", users_guest_sign_in_path, method: :post %>
            </li>
            <li class="px-4">
              <%= link_to "About", about_path %>
            </li>
            </ul>
          </div>
        </div>
      </nav>
    </header>
  <% end %>

  <main class="mb-auto">
    <p id="notice"><%= notice %></p>
    <p id="alert"><%= alert %></p>
      <div class="justify-content-center mb-2">
        <%= render 'layouts/form' %>
      <%= yield %>
      </div>
  </main>
  
    <footer class="bg-dark text-white pt-5 pb-4">
      <div class="container">
        <div class="row no-gutters">
          <div class="col-md-4">
            <%= image_tag('icon.jpg', size: "180x70") %>
          </div>
          <div class="col-md-8">
            <h6 class="text-white mt-2 mb-3">SHARE YOUR LAUGHS.　FIND YOUR FUN.</h6>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
